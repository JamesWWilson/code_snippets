
ls -lh
-- move directory to .ssh
cd .ssh
-- create ssh key
ssh-keygen -t rsa -C "james.wilson@capgemini.com"
-- confirm your address
eval $(ssh-agent)
-- add id
ssh-add id_rsa
-- print id to console + add to bitbucket
cat id_rsa.pub
-- clone repo
git clone repo

